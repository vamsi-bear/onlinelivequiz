<?xml version="1.0" encoding="utf-8"?>

<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- ================================================= -->
        <!-- TITLE -->
        <!-- ================================================= -->

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Create New Quiz"
            android:textSize="27sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:gravity="center"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="24dp" />

        <!-- ================================================= -->
        <!-- QUIZ TITLE -->
        <!-- ================================================= -->

        <EditText
            android:id="@+id/titleEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Quiz Title"
            android:inputType="textCapSentences"
            android:padding="14dp"
            android:layout_marginBottom="12dp" />

        <!-- ================================================= -->
        <!-- DESCRIPTION -->
        <!-- ================================================= -->

        <EditText
            android:id="@+id/descriptionEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Quiz Description"
            android:inputType="textCapSentences|textMultiLine"
            android:minLines="3"
            android:gravity="top"
            android:padding="14dp"
            android:layout_marginBottom="12dp" />

        <!-- ================================================= -->
        <!-- DURATION -->
        <!-- ================================================= -->

        <EditText
            android:id="@+id/durationEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Duration (minutes)"
            android:inputType="number"
            android:padding="14dp"
            android:layout_marginBottom="12dp" />

        <!-- ================================================= -->
        <!-- TOTAL QUESTIONS -->
        <!-- ================================================= -->

        <EditText
            android:id="@+id/totalQuestionsEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Total Questions"
            android:inputType="number"
            android:padding="14dp"
            android:layout_marginBottom="12dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Enter the number of questions. Question fields will appear below."
            android:textSize="14sp"
            android:textColor="@color/black"
            android:layout_marginBottom="12dp" />

        <!-- ================================================= -->
        <!-- MAX ATTEMPTS -->
        <!-- ================================================= -->

        <EditText
            android:id="@+id/maxAttemptsEditText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Allowed Attempts"
            android:inputType="number"
            android:padding="14dp"
            android:layout_marginBottom="20dp" />

        <!-- ================================================= -->
        <!-- QUESTIONS HEADER -->
        <!-- ================================================= -->

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Quiz Questions"
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="8dp" />

        <!-- ================================================= -->
        <!-- QUESTIONS ARE ADDED HERE -->
        <!-- ================================================= -->

        <LinearLayout
            android:id="@+id/questionsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <!-- ================================================= -->
        <!-- CREATE QUIZ BUTTON -->
        <!-- ================================================= -->

        <Button
            android:id="@+id/createQuizButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="CREATE QUIZ"
            android:textSize="17sp"
            android:textStyle="bold"
            android:padding="15dp"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="30dp" />

    </LinearLayout>

</ScrollView>